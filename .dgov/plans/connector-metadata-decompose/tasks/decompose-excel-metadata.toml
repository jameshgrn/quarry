[tasks.decompose-excel-metadata]
summary = "Extract workbook-open, header-read, coord-resolve, and row-scan helpers from _read_excel_metadata in excel_xy.py. Pure refactor — no behavior change."
prompt = """
Orient:
- Read `packages/quarry-connectors/src/quarry_connectors/excel_xy.py` end-to-end. The module-level `_read_excel_metadata(file_path, sheet_name=None, explicit_lon_col=None, explicit_lat_col=None)` function starts at line 157 and is 152 lines, cyclomatic 29, cognitive 44 per sentrux — the top long-function offender in this connector. It runs four sequential phases: (1) validate file existence/size and open the workbook, selecting the requested sheet (or the first sheet) and returning sheet_names; (2) read the header row from the worksheet; (3) resolve `lon_col`/`lat_col`/`crs_hint` either by auto-detection via `_detect_coordinate_columns` or by validating explicit columns against the headers; (4) iterate data rows to count rows, count valid features, accumulate x/y values, and compute the extent.
- Lane: connector (refactor only). One logical commit.
- Doctrine: this is a strict refactor. No public behavior change, no algorithm change, no error-message text change, no error-raising-order change. The existing pressure tests in `tests/pressure_test/test_excel_xy_connector.py` must keep passing without modification. The two callers of `_read_excel_metadata` inside this module (`ExcelXYConnector.materialize`, `ExcelXYConnector.metadata`, and `ExcelXYConnector.discover`) must continue to receive the exact same dict shape with the exact same keys.
- HARD BOUNDARY: only `packages/quarry-connectors/src/quarry_connectors/excel_xy.py` may be MODIFIED. The test file `tests/pressure_test/test_excel_xy_connector.py` is in `files.read` ONLY — it must remain byte-identical to HEAD. Do not open it for editing, do not modify, do not delete. dgov diffs the final commit; any line change in a read-only file is a contract violation. The settlement gate runs the full pressure suite — trust it; do NOT run pytest yourself.

Edit:
1. In `packages/quarry-connectors/src/quarry_connectors/excel_xy.py`, refactor `_read_excel_metadata(...)` so its body becomes orchestration only. Extract these private module-level helpers, placed BEFORE `_read_excel_metadata` (after the existing `_to_float` helper):
   a. `_open_workbook_and_sheet(file_path: Path, sheet_name: str | None) -> tuple[Any, Any, str, list[str]]` — validates `HAS_OPENPYXL`, file existence, non-empty file, opens the workbook via `openpyxl.load_workbook(..., read_only=True, data_only=True)`, selects the requested sheet (closing the workbook and raising `MaterializeError` with the existing "Sheet not found" message on miss) or falls back to `wb.active`, and returns `(wb, ws, resolved_sheet_name, sheet_names)`. Preserve every existing `MaterializeError` message string verbatim and the existing `from e` chaining for the load_workbook failure.
   b. `_read_header_row(wb: Any, ws: Any, file_path: Path) -> list[str]` — wraps the `next(ws.rows)` call, builds the headers list with the existing `str(cell.value) if cell.value is not None else ""` rule, and on `StopIteration` closes the workbook and raises `MaterializeError(str(file_path), "Sheet has no header row")`.
   c. `_resolve_coordinate_columns(headers: list[str], explicit_lon_col: str | None, explicit_lat_col: str | None, wb: Any, file_path: Path) -> tuple[str | None, str | None, str | None]` — implements the existing two-branch logic: when either explicit column is None, call `_detect_coordinate_columns` and fill in the missing side(s); when both are explicit, validate each against `headers` (closing `wb` and raising the existing `MaterializeError` messages on miss) and derive `crs_hint` via `_normalize_column` exactly as today. Returns `(lon_col, lat_col, crs_hint)`.
   d. `_scan_data_rows(ws: Any, headers: list[str], lon_col: str | None, lat_col: str | None, wb: Any, file_path: Path) -> tuple[int, int, tuple[float, float, float, float] | None]` — performs the existing row-iteration logic: when `has_coordinates` is true, look up `lon_idx`/`lat_idx` (closing `wb` and re-raising `MaterializeError` with the existing message on `ValueError`), iterate `ws.iter_rows(min_row=2, values_only=True)`, count rows, skip malformed rows, validate via `_is_numeric`, convert via `_to_float`, accumulate x/y, and compute the `(min, min, max, max)` extent; when `has_coordinates` is false, iterate `ws.iter_rows(min_row=2, values_only=True)` and count rows only. Returns `(row_count, valid_feature_count, extent)`.
2. After extraction, `_read_excel_metadata` reads as: call `_open_workbook_and_sheet`, call `_read_header_row`, call `_resolve_coordinate_columns`, compute `has_coordinates`, call `_scan_data_rows`, `wb.close()`, build and return the result dict with the existing key set (`columns`, `row_count`, `sheet_name`, `sheet_names`, `lon_col`, `lat_col`, `crs_hint`, `has_coordinates`, `valid_feature_count`, `extent`).
3. Helper placement: module-level private functions, type hints required. Use `Any` from `typing` for openpyxl workbook/worksheet types (already imported).
4. Do NOT change: the `_read_excel_metadata` public signature, the returned dict keys or value semantics, `MaterializeError` message strings, the order in which errors are raised, the `wb.close()` placement on the success path, the `_detect_coordinate_columns`/`_normalize_column`/`_is_numeric`/`_to_float`/`_parse_source_ref`/`_write_geopackage` helpers, the `ExcelXYConnector` class, or any other module-level symbol.
5. Do NOT add new validation, new dict keys, new dataclass fields, logging, or any "while you're here" cleanup. Do not introduce a dataclass for the metadata dict — keep the return type `dict[str, Any]`.

Verify:
- `uv run ruff check packages/quarry-connectors/src/quarry_connectors/excel_xy.py`
- `uv run ruff format --check packages/quarry-connectors/src/quarry_connectors/excel_xy.py`
- Quick syntax check: `uv run python -c "from quarry_connectors.excel_xy import _read_excel_metadata, _open_workbook_and_sheet, _read_header_row, _resolve_coordinate_columns, _scan_data_rows; print('ok')"`
- The settlement gate runs the full pressure suite — trust it; do NOT run pytest yourself.
"""
commit_message = "quarry-connectors: extract excel metadata helpers"
self_review = true

files.edit = ["packages/quarry-connectors/src/quarry_connectors/excel_xy.py"]
files.read = ["tests/pressure_test/test_excel_xy_connector.py"]
