[tasks.decompose-watershed-main]
summary = "Decompose main() in examples/watershed_analysis.py into module-level phase helpers (ingest, process, analyze, export, inspect). Pure refactor — no behavior change."
prompt = """
Orient:
- Read `examples/watershed_analysis.py` end-to-end. The module docstring documents five phases (INGEST, PROCESS, ANALYZE, EXPORT, INSPECT) and `main()` (line 108, 183 lines) implements them inline as five labeled blocks separated by `# ── N. PHASE ──` banners.
- Lane: example (refactor only). Treat as `adapter` for lane semantics. One logical commit.
- Doctrine: this is a strict refactor of the canonical end-to-end example referenced in AGENTS.md (CLAUDE.md is a symlink to it). The example must still execute end-to-end, produce the same registry state, and print the same final summary. No new features, no comment churn, no "while you're here" cleanup.
- HARD BOUNDARY: only `examples/watershed_analysis.py` may be MODIFIED. The file imports from `quarry_connectors`, `quarry_core`, `quarry_operators`, `quarry_registry` — those packages are READ-ONLY context. dgov diffs the final commit; any line change in a read-only file is a contract violation.
- Existing helpers `create_synthetic_dem` and `create_zone_polygons` must remain BYTE-FOR-BYTE unchanged. Do not touch the module docstring, imports, or the `if __name__ == "__main__"` block.

Edit:
1. In `examples/watershed_analysis.py`, refactor `main()` so its body becomes orchestration only: set up the workspace, then call five phase helpers in order, threading state through their return values. The five `# ── N. PHASE ──` banners and their print blocks must move INTO the helpers verbatim — preserve every print statement, format string, and ordering exactly.
2. Add these private module-level helpers, placed AFTER `create_zone_polygons` and BEFORE `main`, with full type hints:
   a. `_ingest_phase(workspace: Path, data_dir: Path) -> tuple[Artifact, Artifact]` — creates synthetic DEM + zones, builds the default router, materializes both through connectors, prints the INGEST banner + artifact summaries, returns `(dem_artifact, zones_artifact)`.
   b. `_process_phase(workspace: Path, hydro_dir: Path, dem_artifact: Artifact, zones_artifact: Artifact) -> tuple[LocalExecutor, Registry, HydrologyFlowResult]` — instantiates `LocalExecutor` and `Registry`, saves input artifacts, runs `HydrologyFlow`, exits on failure, prints the PROCESS banner + check summary, returns `(executor, registry, flow_result)`. (Use the actual return type of `HydrologyFlow.run` — import the type only if it's already imported; otherwise reference via `flow_result`'s inferred type or `object` rather than adding a new import.)
   c. `_analyze_phase(workspace: Path, executor: LocalExecutor, registry: Registry, flow_result, zones_artifact: Artifact) -> Artifact` — runs `ZonalStatsOperator` via the executor, exits on failure, saves the run, reads the CSV through the artifact's backing store, prints the ANALYZE banner + per-zone summary, returns `zonal_artifact`.
   d. `_export_phase(export_dir: Path, executor: LocalExecutor, registry: Registry, flow_result) -> Artifact` — runs `BuildCOGOperator` via the executor, exits on failure, saves the run, prints the EXPORT banner + COG summary, returns `cog_artifact`.
   e. `_inspect_phase(registry: Registry, cog_artifact: Artifact, zonal_artifact: Artifact) -> None` — prints the INSPECT banner, registry stats, both lineage chains, and the final "Done." footer.
3. `main()` reads as: create workspace + dirs, print `Workspace: ...`, then `dem_artifact, zones_artifact = _ingest_phase(...)`, `executor, registry, flow_result = _process_phase(...)`, `zonal_artifact = _analyze_phase(...)`, `cog_artifact = _export_phase(...)`, `_inspect_phase(...)`. Nothing else.
4. Keep `import tempfile` inside `main()` where it currently lives. Do NOT promote it to a top-level import. Do NOT add or remove any other imports unless a helper signature strictly requires it (e.g., `Artifact`); if you do add one, add it at the top of the existing import block in alphabetical order.
5. Do NOT change: the module docstring, existing helper bodies (`create_synthetic_dem`, `create_zone_polygons`), the order of phases, any printed line, the `sys.exit(1)` failure paths, the registry interactions, or the `if __name__ == "__main__"` guard.
6. Do NOT add: new validation, new comments beyond what's already there, type aliases, dataclasses, or any cleanup.

Verify:
- `uv run ruff check examples/watershed_analysis.py`
- `uv run ruff format --check examples/watershed_analysis.py`
- Syntax check: `uv run python -m py_compile examples/watershed_analysis.py`
- The settlement gate runs the full pressure suite — do NOT run pytest yourself.
"""
commit_message = "examples: decompose watershed_analysis main into phase helpers"
self_review = true

files.edit = ["examples/watershed_analysis.py"]
files.read = ["AGENTS.md"]
