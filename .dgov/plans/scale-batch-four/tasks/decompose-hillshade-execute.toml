[tasks.decompose-hillshade-execute]
summary = "Decompose HillshadeOperator.execute in hillshade.py into module-level phase helpers (DEM read, gradient/slope/aspect, illumination, write). Pure refactor — no behavior change."
prompt = """
Orient:
- Read `packages/quarry-operators/src/quarry_operators/hillshade.py` end-to-end. `HillshadeOperator.execute` starts at line 139 and is 175 lines (line_count=175 per sentrux) — the top long-function offender in this module. It opens a single-band DEM with rasterio, builds a validity mask, computes central-difference gradients, derives slope/aspect, applies the Horn (1981) illumination formula, writes uint8 or float64 output, then assembles the output artifact and runs declared checks.
- Lane: operator (refactor only). One logical commit.
- Doctrine: this is a strict refactor. No public behavior change. The existing pressure tests in `tests/pressure_test/test_hillshade.py` must keep passing without modification (output shape, illumination semantics, edge handling for 1-row/1-col DEMs, nodata propagation, lineage params, scaled vs uint8 dtype).
- HARD BOUNDARY: only `packages/quarry-operators/src/quarry_operators/hillshade.py` may be MODIFIED. The test file `tests/pressure_test/test_hillshade.py` is in `files.read` ONLY — do not open for editing, do not modify, do not delete. dgov diffs the final commit; any line change in a read-only file is a contract violation. The settlement gate runs the full pressure suite — trust it; do NOT run pytest yourself.

Edit:
1. In `packages/quarry-operators/src/quarry_operators/hillshade.py`, refactor `HillshadeOperator.execute(...)` so its body becomes orchestration only. Extract these private module-level helpers, placed BEFORE the `HillshadeOperator` class (after `HillshadeParams`):
   a. `_read_dem(input_path: str, op_name: str, artifact_id: str, params_nodata: float | None) -> tuple[np.ndarray, float | None, dict, object]` — open the raster, enforce single-band (raise `OperatorError` on violation), return `(dem_float64, resolved_nodata, meta_copy, transform)`.
   b. `_compute_slope_aspect(dem: np.ndarray, valid: np.ndarray, cell_width: float, cell_height: float, z_factor: float) -> tuple[np.ndarray, np.ndarray]` — apply NaN mask + z_factor scaling, run central-difference gradients with the existing 1-row / 1-col / general branches, return `(slope_rad, aspect_rad)`.
   c. `_compute_illumination(slope_rad: np.ndarray, aspect_rad: np.ndarray, valid: np.ndarray, azimuth_deg: float, altitude_deg: float, output_nodata: float) -> np.ndarray` — convert sun angles, apply Horn (1981) formula, clip to [0,1], replace `~valid | nan_mask` with `output_nodata`, return illumination array.
   d. `_write_hillshade(illumination: np.ndarray, output_path: Path, meta: dict, scaled: bool, output_nodata: float) -> tuple[np.ndarray, str, float | int]` — pick uint8 vs float64 dtype, scale to 0-255 if needed, update meta, write the raster, return `(hillshade_array, out_dtype, out_nodata)`.
2. After extraction, `execute` reads as: validate params type, resolve paths, call `_read_dem`, build validity mask, validate cell dimensions, call `_compute_slope_aspect`, call `_compute_illumination`, call `_write_hillshade`, build output artifact (existing inline block), run checks, return `OperatorResult`. Keep the `try/except OperatorError/Exception` envelope wrapping the compute+write phases as it is today.
3. Helper placement: module-level private functions, type hints required. Imports (`rasterio`, `numpy`) stay where they are; the inline `import rasterio` inside `execute` may stay inside `execute` (do not relocate to keep diff minimal — orchestration may still re-import, or move to module scope only if already module-scoped).
4. Do NOT change: the public `execute` signature, `HillshadeParams`, `validate_inputs`, `_run_checks`, `declared_checks`, the Horn (1981) formula, the gradient branching for 1-row / 1-col / general DEMs, the lineage `params` dict contents, the artifact metadata dict contents, or any error message text.
5. Do NOT add: new validation, new params fields, new checks, or any "while you're here" cleanup.

Verify:
- `uv run ruff check packages/quarry-operators/src/quarry_operators/hillshade.py`
- `uv run ruff format --check packages/quarry-operators/src/quarry_operators/hillshade.py`
- Quick syntax check: `uv run python -c "from quarry_operators.hillshade import HillshadeOperator, _read_dem, _compute_slope_aspect, _compute_illumination, _write_hillshade; print('ok')"`
- The settlement gate runs the full pressure suite — trust it; do NOT run pytest yourself.
"""
commit_message = "quarry-operators: extract hillshade execute helpers"
self_review = true

files.edit = ["packages/quarry-operators/src/quarry_operators/hillshade.py"]
files.read = ["tests/pressure_test/test_hillshade.py"]
